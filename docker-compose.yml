services:

  api:
    build: .

    container_name: fastapi_api

    ports:
      - "8000:8000"

    env_file:
      - .env

    depends_on:
      - redis

    restart: always

  redis:
    image: redis:7

    container_name: redis_cache

    restart: always